<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <data>

        <variable
            name="videoInfo"
            type="com.woyou.zxbrowser.VideoInfo"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/ripple_white_back"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:layout_marginStart="10dp"
            android:onClick="@{videoInfo::onClick}"
            android:background="@drawable/ripple_trasparent_back"
            android:src="@drawable/download"/>

        <View
            android:layout_width="0.2dp"
            android:layout_height="40dp"
            android:background="@color/home_divide"/>

        <TextView
            android:id="@+id/video_item_url"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:clickable="false"
            android:focusable="false"
            android:singleLine="true"
            android:text="@{videoInfo.url}"/>
    </LinearLayout>
</layout>